git init .
git add .
git diff = compare changes WorkingDIrectory vs StagingArea
git diff --staged = compare SA vs LocalRepo
git diff HEAD = compare WD vs LR

Creat account on github alex-u2-demo 
invit alex-u2-demo to access repo git   

git annotate file2
git show HEAD
git diff HEAD HEAD~2

Fork permet de copier sur son repo github un projet tiers public
fork de github à github
les modifications seront proposé en Pull requets
clone github à repo local

si modifie un repo public alors demande pull requeste

si ajout un collaborateur, il peut push sans pull request

git tag v1.0
git push origin v1.0
git tag -a v0.1 f2835356f0 -m "custom tag commit"
git push --all
git push --tags



